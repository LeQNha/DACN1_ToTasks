<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base Theme dùng MaterialComponents -->
    <style name="Base.Theme.ToTasks" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/button_background_color</item>
        <item name="colorPrimaryVariant">@color/button_background_color</item> <!-- Thay vì colorPrimaryDark -->
        <item name="colorSecondary">@color/colorAccent</item> <!-- Thay vì colorAccent (deprecated) -->
        <item name="windowActionModeOverlay">true</item>
        <item name="android:actionModeBackground">@color/black</item>
    </style>

    <!-- App Theme kế thừa từ Base -->
    <style name="Theme.ToTasks" parent="Base.Theme.ToTasks" />
</resources>